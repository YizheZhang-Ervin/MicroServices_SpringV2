FROM tomcat:jdk8
LABEL maintainer=ervin

ENV APP_FILE=client-b.war
ENV APP_HOME=$CATALINA_HOME/webapps
ENV CONF_HOME=$CATALINA_HOME/conf
ENV SERVER_PORT=8080

COPY $APP_FILE $APP_HOME
COPY server.xml $CONF_HOME

WORKDIR $APP_HOME
EXPOSE $SERVER_PORT

CMD ["catalina.sh","run"]